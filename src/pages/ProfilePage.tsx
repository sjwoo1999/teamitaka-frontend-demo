import styled from "styled-components";

const ProfileContainer = styled.div`
  padding: 20px;
  text-align: center;
`;

export const ProfilePage: React.FC = () => {
  return <ProfileContainer>프로필 페이지입니다.</ProfileContainer>;
};